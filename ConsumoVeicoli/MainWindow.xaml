<Window x:Class="ConsumoVeicoli.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Consumi" Height="450" Width="600">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Riga 0 -->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="btnToggleAll"
                    Grid.Column="0"
                    Content="Seleziona Tutte"
                    Width="100" Height="25"
                    FontSize="10"
                    Margin="0,0,10,0"
                    Click="btnToggleAll_Click"/>

            <StackPanel Orientation="Horizontal" Grid.Column="1">
                <!-- modalità di ricerca -->
                <ComboBox x:Name="cbModoRicerca" Width="120" Margin="0,54,10,54"
                          SelectionChanged="cbModoRicerca_SelectionChanged">
                    <ComboBoxItem Content="Targhe"/>
                    <ComboBoxItem Content="Autisti"/>
                </ComboBox>

                <TextBlock x:Name="lblRisorsa" Text="Targhe:" VerticalAlignment="Center"/>

                <TextBox x:Name="tbSearch"
                    Width="150"
                    Margin="5,54,0,54"
                    TextChanged="tbSearch_TextChanged"
                    ToolTip="Digita per filtrare…"/>

                <ListBox x:Name="lbRisorse"
                         SelectionMode="Multiple"
                         Margin="10,0,0,0"
                         MaxHeight="200"
                         Width="1050"
                         ScrollViewer.HorizontalScrollBarVisibility="Auto"
                         ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Rows="6"/>
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding}"
                                      IsChecked="{Binding IsSelected,
                                                  RelativeSource={RelativeSource AncestorType=ListBoxItem},
                                                  Mode=TwoWay}"/>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </StackPanel>
        </Grid>

        <!-- Riga 1 -->
        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,10,0,10">
            <RadioButton x:Name="rbStessoPeriodo"      Content="Stesso periodo"     IsChecked="True" Margin="0,0,30,0"/>
            <RadioButton x:Name="rbPeriodiDifferenti"  Content="Periodi differenti"/>
        </StackPanel>

        <!-- Riga 2 -->
        <DataGrid x:Name="dgRisultati"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"/>

        <!-- Riga 3 -->
        <StackPanel Orientation="Horizontal" Grid.Row="3" HorizontalAlignment="Right">
            <Button x:Name="btnCarica"  Content="Carica"         Width="80"  Margin="5" Click="btnCarica_Click"/>
            <Button x:Name="btnEsporta" Content="Esporta Excel"  Width="100" Margin="5" Click="btnEsporta_Click"/>
        </StackPanel>
    </Grid>
</Window>
