<Window x:Class="ConsumoVeicoli.FinestraDate"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Periodo Unico" Width="300" Height="180" WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <Label Content="Da:" VerticalAlignment="Center"/>
            <DatePicker x:Name="dpDa" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1">
            <Label Content="A:" VerticalAlignment="Center"/>
            <DatePicker x:Name="dpA" Margin="10,0,0,0"/>
        </StackPanel>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="2">
            <Button x:Name="btnOk" Content="OK" Width="60" Margin="5" Click="btnOk_Click"/>
            <Button x:Name="btnCancel" Content="Annulla" Width="60" Margin="5" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
